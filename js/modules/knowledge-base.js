export function renderKnowledgeBase(container) {
    container.innerHTML = `<h2>Knowledge Base</h2><p style="color:var(--text-muted); margin-top:1rem;">Access stored documentation and vectors.</p>`;
}
