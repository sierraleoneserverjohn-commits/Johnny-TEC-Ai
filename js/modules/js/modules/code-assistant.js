export function renderCodeAssistant(container) {
    container.innerHTML = `<h2>Code Assistant</h2><p style="color:var(--text-muted); margin-top:1rem;">Write, refactor, and debug code instantly.</p>`;
}
